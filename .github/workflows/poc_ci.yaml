name: test_ci

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test_0
        run: |
          a=$(git log --date=iso --date=format:"%Y/%m/%d" --pretty=format:"%ad" -1)
          echo ${#a}
      - name: test_1
        run: |
          b=$(echo $(cat update_dates/date_site.txt))
          echo ${#b}
      - name: Check Date
        run: |
          date_commit=$(git log --date=iso --date=format:"%Y/%m/%d" --pretty=format:"%ad" -1)
          date_txt=$(echo $(cat update_dates/date_site.txt) | cut -c 1-10)
          if [[ $date_commit != $date_txt ]]; then exit 1; fi
